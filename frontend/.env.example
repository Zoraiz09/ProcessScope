# Base URL of the deployed FastAPI backend (no trailing slash).
# Leave UNSET for local dev — relative paths use the Vite proxy in vite.config.ts.
# In production (Vercel), set this to your backend origin:
#   VITE_API_BASE=https://processscope-api.onrender.com
VITE_API_BASE=
